{
  "name": "ai-code-review-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "build:contracts": "yarn workspace @ai-code-review-platform/contracts build",
    "dev:gateway": "yarn workspace api-gateway dev",
    "dev:processor": "yarn workspace code-processor dev",
    "dev": "yarn build:contracts && yarn dev:gateway && yarn dev:processor"
  }
}
